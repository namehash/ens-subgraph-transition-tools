{
	"name": "api-eq",
	"private": true,
	"module": "index.ts",
	"type": "module",
	"scripts": {
		"start": "bun run --env-file ../../.env.local index.ts",
		"dev": "bun run --env-file ../../.env.local --watch index.ts"
	},
	"dependencies": {
		"eq-lib": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}
